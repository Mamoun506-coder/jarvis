# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Copied out of node_modules by scripts/start.mjs — 34MB of build output.
public/mediapipe/

# Google OAuth client and tokens live in ~/.jarvis, never here — but if a
# stray copy is ever made in the repo, it must not be committed.
google-client.json
google-tokens.json
